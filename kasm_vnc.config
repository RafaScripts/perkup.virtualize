[server]
# Conecta ao servidor VNC que a VM do macOS irá expor na porta 5999
vnc_host = 127.0.0.1
vnc_port = 5999

# Desativa a autenticação do KasmVNC (o Kasm Workspaces cuidará disso mais tarde )
# Para teste local, isso simplifica o acesso.
require_auth = false
